apiVersion: v1
kind: Service
metadata:
  name: {{ include "rcabench-frontend.fullname" . }}
  labels:
    {{- include "rcabench-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rcabench-frontend.selectorLabels" . | nindent 4 }}
